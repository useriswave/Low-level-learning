#ifndef SORTER_H
#define SORTER_H
#include <stdio.h>

void int_bubble_sort(int *arr, const size_t len);
void selection_sort(int *arr, const size_t len);

#endif /* ifndef SORTER_H
#define SORTER_H */
